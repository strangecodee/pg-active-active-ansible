- name: Active-Active PostgreSQL 16 Platform
  hosts: postgres
  become: yes
  roles:
    - common
    - postgres
    - pglogical
    - dns
    - ownership
    - fdw
    - haproxy
    # keepalived and fencing disabled when using DNS routing instead of VIP
    # - keepalived
    # - fencing
